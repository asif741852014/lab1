name: abcd
on:
    push: 
jobs:
    abcdef:
        runs-on: ubuntu-latest
        name: avc
        steps:
        - uses: actions/checkout@v4
        - run: ls -lrth
        - name: kkk
          uses: ./
          with:
            demo: "asif first"